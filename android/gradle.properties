# Project-wide Gradle settings
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8 -XX:+UseParallelGC -XX:+HeapDumpOnOutOfMemoryError
org.gradle.parallel=true
org.gradle.configureondemand=true
org.gradle.daemon=true

# Android settings
android.useAndroidX=true
android.enableJetifier=true

# React Native settings
hermesEnabled=true
newArchEnabled=false

# Enable R8 for better optimization
android.enableR8.fullMode=true

# ✅ Ensure Kotlin uses JVM 17 (fixes mismatch issue)
kotlin.jvm.target=17

# If you're behind a corporate firewall, uncomment and configure these:
# systemProp.http.proxyHost=your-proxy-host
# systemProp.http.proxyPort=your-proxy-port
# systemProp.https.proxyHost=your-proxy-host
# systemProp.https.proxyPort=your-proxy-port
